## Changes

- Fixed an `AbstractMethodError` during startup with Fabric Loader 0.19.3 and other supported older Loader versions. This affected modpacks where C2ME triggered early Mixin loading; ECO now explicitly implements the plugin methods required by older Mixin versions.
