## Changes

- Simplified the native spatial index and collision query path while preserving vanilla entity order.
- Removed the unordered backend and the `vanillaOrder` and `gridSize` settings.
- Expanded native query and index parity coverage.
